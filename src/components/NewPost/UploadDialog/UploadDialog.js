import React from 'react';

import Dialog from '@material-ui/core/Dialog';

function UploadDialog(props){
    return (
        <Dialog open={props.isUploadDialogOpen}>

        </Dialog>
    )
}

export default UploadDialog;